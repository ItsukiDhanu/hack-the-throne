# Choose one storage backend
# Vercel KV (recommended on Vercel)
KV_REST_API_URL=
KV_REST_API_TOKEN=
KV_REST_API_READ_ONLY_TOKEN=

# OR Redis URL
REDIS_URL=

# Admin token for POST /api/content and (if set) GET /api/registrations
ADMIN_TOKEN=
